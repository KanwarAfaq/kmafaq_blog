import { ArrowUpRight, Sparkles } from 'lucide-react';
import { useEffect, useState } from 'react';
import SEO from '../components/SEO';
import { getAffiliateTools } from '../lib/api';

export default function ToolsDirectoryPage(){const [tools,setTools]=useState([]);useEffect(()=>{getAffiliateTools().then(setTools).catch(()=>setTools([]));},[]);return <main className="py-14"><SEO title="AI Tools Directory" path="/tools" description="Useful AI and software tools selected by KM Afaq. Some links may be affiliate links."/><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><p className="text-sm font-black text-secondary">TOOLS & RESOURCES</p><h1 className="mt-2 text-4xl font-black text-ink">AI tools people can actually use</h1><p className="mt-3 max-w-2xl text-gray-600">Curated software with tracked outbound links. Add genuine affiliate programs here to earn commission from qualified traffic.</p><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{tools.map((t)=><article key={t.id} className="rounded-3xl border border-gray-200 bg-white p-6 shadow-sm">{t.badge?<span className="rounded-full bg-violet-50 px-3 py-1 text-xs font-black text-secondary">{t.badge}</span>:null}<div className="mt-4 flex items-center gap-3">{t.logo_url?<img src={t.logo_url} alt="" className="h-11 w-11 rounded-xl object-contain"/>:<span className="flex h-11 w-11 items-center justify-center rounded-xl bg-violet-50 text-secondary"><Sparkles/></span>}<div><h2 className="font-black text-ink">{t.name}</h2><p className="text-xs font-bold text-gray-400">{t.category}{t.pricing_label?` · ${t.pricing_label}`:''}</p></div></div><p className="mt-4 text-sm leading-6 text-gray-600">{t.description}</p><a href={`/api/go?type=tool&id=${encodeURIComponent(t.id)}`} target="_blank" rel="noreferrer sponsored" className="mt-5 inline-flex items-center gap-1 font-black text-primary">Visit tool <ArrowUpRight className="h-4 w-4"/></a></article>)}{!tools.length?<div className="col-span-full rounded-3xl border border-dashed border-gray-300 p-10 text-center text-gray-500"><p className="font-black text-ink">Tools directory is ready.</p><p className="mt-2 text-sm">Add real affiliate programs to <code>affiliate_tools</code> after the migration.</p></div>:null}</div><p className="mt-8 text-xs text-gray-400">Disclosure: some tool links may be affiliate links, which can earn KM Afaq a commission at no extra cost to the visitor.</p></div></main>}
