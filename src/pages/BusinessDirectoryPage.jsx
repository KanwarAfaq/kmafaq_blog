import { ArrowUpRight, BadgeCheck, Building2 } from 'lucide-react';
import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { getBusinessListings } from '../lib/api';

export default function BusinessDirectoryPage(){const [rows,setRows]=useState([]);useEffect(()=>{getBusinessListings().then(setRows).catch(()=>setRows([]));},[]);return <main className="py-14"><SEO title="Business Directory" path="/business" description="Discover trusted businesses and service providers featured on KM Afaq."/><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-black text-primary">BUSINESS DIRECTORY</p><h1 className="mt-2 text-4xl font-black text-ink">Discover services. Generate real leads.</h1><p className="mt-3 max-w-2xl text-gray-600">Featured businesses receive priority placement and direct customer inquiries.</p></div><Link to="/pricing" className="rounded-xl bg-primary px-5 py-3 text-center font-black text-white">List your business</Link></div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{rows.map((b)=><article key={b.id} className="rounded-3xl border border-gray-200 bg-white p-6 shadow-sm">{b.featured?<span className="inline-flex items-center gap-1 rounded-full bg-amber-50 px-3 py-1 text-xs font-black text-amber-700"><BadgeCheck className="h-3.5 w-3.5"/> Featured</span>:null}<div className="mt-4 flex items-center gap-3">{b.logo_url?<img src={b.logo_url} alt="" className="h-12 w-12 rounded-xl object-cover"/>:<span className="flex h-12 w-12 items-center justify-center rounded-xl bg-blue-50 text-primary"><Building2/></span>}<div><h2 className="font-black text-ink">{b.name}</h2><p className="text-xs font-bold text-gray-400">{b.category}{b.city?` · ${b.city}`:''}</p></div></div><p className="mt-4 text-sm leading-6 text-gray-600">{b.short_description}</p><Link to={`/business/${b.slug}`} className="mt-5 inline-flex items-center gap-1 font-black text-primary">View & request quote <ArrowUpRight className="h-4 w-4"/></Link></article>)}{!rows.length?<div className="col-span-full rounded-3xl border border-dashed border-gray-300 p-10 text-center"><p className="font-black text-ink">Directory ready for your first paid listings.</p><Link to="/pricing" className="mt-3 inline-block text-sm font-black text-primary">Sell a featured listing</Link></div>:null}</div></div></main>}
